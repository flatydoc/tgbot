BOT_TOKEN=your_telegram_bot_token_from_botfather
# Через запятую — Telegram user id администраторов (узнать у @userinfobot)
ADMIN_IDS=123456789

# Деплой: каталог для SQLite (постоянный том обязателен на Railway и др.)
# DATA_DIR=/data

# Railway/Render задают PORT — поднимется HTTP «ok» для healthcheck
# PORT задаётся платформой, не прописывайте вручную

# --- Railway ---
# В проекте есть railway.toml (Dockerfile). В UI добавьте:
# BOT_TOKEN, ADMIN_IDS, DATA_DIR=/data
# Volume: mount path = /data (совпадает с DATA_DIR),719509466
